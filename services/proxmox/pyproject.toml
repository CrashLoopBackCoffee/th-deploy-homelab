[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"

[project]
name = "proxmox"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = ["ansible>=11.1.0", "utils"]

[tool.uv.build-backend]
module-name = "proxmox"
module-root = ""
